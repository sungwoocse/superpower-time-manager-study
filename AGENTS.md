# Project Agent Instructions

## Workflow

Implementation is currently paused until the user explicitly resumes it.

When implementation resumes, follow the approved plan in `docs/superpowers/plans/2026-05-12-time-manager-mvp-implementation.md`.

`docs/solutions/` contains documented solutions to past bugs, workflow issues, and implementation decisions, organized by category with YAML frontmatter fields such as `module`, `tags`, and `problem_type`. It is relevant background when implementing or debugging in documented areas.

After the final implementation review is complete, run the Compound Engineering process before closing the work:

```text
ce-compound mode:headless "time manager MVP implementation review cycle learnings"
```

Capture mistakes, review findings, fixes, and process lessons from the execution-review cycle so future work does not repeat them.
